# --- MISSION NETWORKING ---
# Remote backend host (optional). Default prod: iron-sight-hjwf.onrender.com.
# Prod build: REST via Vercel /api rewrite; WS uses this host.
# Dev: unset = Vite proxy (http://localhost:5173/api → :8080).
# localhost:8080 = direct http/ws to local backend. Remote host = https/wss to Render.
# VITE_WS_URL=localhost:8080
# VITE_WS_URL=iron-sight-hjwf.onrender.com
VITE_MISSION_KEY=your_secret_tactical_key_here

# --- WEB PUSH ---
# Must match backend VAPID_PUBLIC_KEY. Generate pair: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=your_vapid_public_key_here

# --- SEO (canonical, OG, sitemap) ---
# Production default: https://iron-sight-drab.vercel.app
VITE_SITE_URL=https://iron-sight-drab.vercel.app
